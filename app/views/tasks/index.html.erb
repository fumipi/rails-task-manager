  <h1>My Tasks</h1>
  <ul>
  <% @tasks.each do |task| %>
    <li>
    <h3 id = dangers><%= link_to "Delete", task_path(task),
        method: :delete,
        data: { confirm: "Are you sure?" } %></h3>
    <h3>&nbsp;<%=link_to task.title, task_path(task) %></h3>
    <h3>&nbsp;completed?&nbsp;<%= task.completed%></h3>
  </li>
  <%end%>
  </ul>

<%= link_to "Create new task", new_task_path %>
